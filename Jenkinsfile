pipeline {
    agent { label 'jenkins_Agent' }
    tools {
        jdk 'java17'
        maven 'Maven3'
    }
    stages {
        stage ('clean Up WorkSpace') {
            steps {
                cleanWs()
            }
        }
      stage ('Clone the gitHub') {
         steps {
           
         }       
      }
    }
}
